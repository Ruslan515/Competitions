import pandas as pd

date = pd.Timestamp("today").date() - pd.DateOffset(days=1)

s = "hello"
print(date)
s = (
    print(
        float(
            (32 - s[
                0]
             )
        )
    )
)